name: mayr_config_example
description: An example app for demonstrating the features of MayrConfig
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.9.2

dependencies:
  mayr_config:
    path: ../

dev_dependencies:
  build_runner: ^2.4.15
  lints: ^6.0.0
  test: ^1.25.6
